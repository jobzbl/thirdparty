import { FlowchartSiderDirective } from './flowchart-sider.directive';

describe('FlowchartSiderDirective', () => {
  it('should create an instance', () => {
    // const directive = new FlowchartSiderDirective();
    // expect(directive).toBeTruthy();
  });
});
